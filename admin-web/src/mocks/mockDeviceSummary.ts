import type { DeviceSummaryResponse } from "../types/type";

export const mockDeviceSummary: DeviceSummaryResponse = {
  totalDevices: 15,
  onlineDevices: 5,
  unverifiedDevices: 5,
  trustedDevices: 5,
};
